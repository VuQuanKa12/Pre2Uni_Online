<div class="nav-item dropdown">
	<a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">Pages</a>
	<div class="dropdown-menu m-0">
		<a href="@Url.Action("Index", "Dictionary", new {searchDictionary = "Welcome"})" class="dropdown-item">Dictionary</a>
		@if (ViewBag.login == true)
		{
		if (ViewBag.Role == 3)
		{
		<a href="@Url.Action("ListExamTestStudent", "ExamTest")" class="dropdown-item" ; font-weight: bold;">Exam Test</a>
		}
		}
		<a href="@Url.Action("Calendar", "Home")" class="dropdown-item">Calendar</a>
		<a href="@Url.Action("Quiz", "Quiz", new {courseChoise=1})" class="dropdown-item">Quiz</a>
	</div>
</div>	</div>

@if (ViewBag.login == true)
{
	if (ViewBag.Role == 2)
	{
		<a href="@Url.Action("ListExamTest", "ExamTest")" class="nav-item nav-link" style="color:white ; font-weight: bold;">Exam Test</a>
	}
	<a href="@Url.Action("Profile", "Home")" class="nav-item nav-link">@ViewBag.user.FirstName @ViewBag.user.LastName</a>
	<a href="@Url.Action("Logout", "Login")" class="btn btn-primary rounded-pill py-2 px-4">Logout</a>
}
else
{
	<a href="@Url.Action("Index", "Login")" class="btn btn-primary rounded-pill py-2 px-4">Join now</a>
}
		</div>
	</nav>
</div>
<!-- Navbar & Hero End -->